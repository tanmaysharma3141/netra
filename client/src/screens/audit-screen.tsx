import { PlaceholderScreen } from "./placeholder-screen"

export function AuditScreen() {
  return (
    <PlaceholderScreen
      title="Audit Log"
      description="Immutable system event log — every action, timestamped and attributed."
      phase="PHASE 5 · CHAT + REPORTS + POLISH (Admin/Supervisor only)"
    />
  )
}
